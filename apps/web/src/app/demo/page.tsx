"use client";

import Link from "next/link";
import { formatEther } from "viem";
import { useBalance, useReadContract } from "wagmi";
import { ADDRESSES, ASSET_STATE_NAMES, ASSET_TYPE_NAMES, assetsAbi, credentialsAbi } from "@/lib/civora";

const DEMO_ASSET_ID = 1n;

export default function DemoPage() {
  const asset = useReadContract({ address: ADDRESSES.assets, abi: assetsAbi, functionName: "assets", args: [DEMO_ASSET_ID] });
  const underwrite = useReadContract({ address: ADDRESSES.credentials, abi: credentialsAbi, functionName: "underwrites", args: [DEMO_ASSET_ID] });
  const monitor = useReadContract({ address: ADDRESSES.credentials, abi: credentialsAbi, functionName: "monitors", args: [DEMO_ASSET_ID] });
  const vault = useBalance({ address: ADDRESSES.vault });
  const exists = Boolean(asset.data?.[0]);
  const state = asset.data ? Number(asset.data[11]) : null;

  return <main className="mx-auto flex min-h-full max-w-[900px] flex-col gap-8 px-6 py-16"><header><p className="font-mono text-xs uppercase tracking-widest text-text-secondary">Civora · live proof · BOT Chain 677</p><h1 className="mt-4 font-grotesk text-4xl font-semibold tracking-tight">AI-operated sustainability-linked settlement.</h1><p className="mt-3 max-w-2xl text-text-secondary">A public, wallet-free view of the primary Civora green-asset contract path. Connect a wallet to issue your own asset.</p></header>{!exists ? <section className="rounded-md border border-border bg-surface p-5"><p className="font-mono text-sm text-text-secondary">Demo asset #1 has not been issued yet.</p><Link href="/app/assets?new=1" className="mt-4 inline-flex h-9 items-center bg-accent px-3 font-grotesk text-sm text-text-on-accent">Issue demo asset</Link></section> : <><section className="grid grid-cols-1 gap-3 sm:grid-cols-3"><div className="rounded-md border border-border bg-surface p-4"><p className="text-xs uppercase tracking-widest text-text-secondary">Asset</p><p className="mt-2 font-mono text-xl">#{DEMO_ASSET_ID.toString()}</p><p className="mt-1 text-xs text-text-secondary">{ASSET_TYPE_NAMES[Number(asset.data![2]) as 1 | 2]}</p></div><div className="rounded-md border border-border bg-surface p-4"><p className="text-xs uppercase tracking-widest text-text-secondary">Escrow</p><p className="mt-2 font-mono text-xl">{formatEther(asset.data![3] + asset.data![4])} BOT</p><p className="mt-1 text-xs text-text-secondary">principal + coupon</p></div><div className="rounded-md border border-border bg-surface p-4"><p className="text-xs uppercase tracking-widest text-text-secondary">State</p><p className="mt-2 font-mono text-xl">{state ? ASSET_STATE_NAMES[state as 1 | 2 | 3 | 4 | 5 | 6] : "…"}</p><p className="mt-1 text-xs text-text-secondary">vault: {vault.data ? `${formatEther(vault.data.value)} BOT` : "…"}</p></div></section><section className="rounded-md border border-border bg-surface p-5"><h2 className="font-grotesk text-sm font-medium">Decision path</h2><ol className="mt-4 flex flex-col gap-4 font-mono text-xs"><li><strong>1. Issued</strong><span className="ml-3 text-text-secondary">target {asset.data ? asset.data[5].slice(0, 10) : "…"}…</span></li><li><strong>2. Underwritten</strong><span className="ml-3 text-text-secondary">{underwrite.data?.[3] === 1 ? "approved" : underwrite.data?.[3] === 2 ? "rejected" : "pending"}</span></li><li><strong>3. Monitored</strong><span className="ml-3 text-text-secondary">{monitor.data?.[3] === 1 ? "target met" : monitor.data?.[3] === 2 ? `target missed · ${monitor.data[4].toString()} bps` : "pending"}</span></li><li><strong>4. Settled</strong><span className="ml-3 text-text-secondary">principal 100% holder · coupon fees only</span></li></ol></section></>}<section className="flex flex-wrap gap-3"><Link href="/" className="font-mono text-xs text-accent">← Civora landing</Link><Link href="/app/assets?new=1" className="font-mono text-xs text-accent">Connect wallet to run the flow →</Link><a href={`https://scan.botchain.ai/address/${ADDRESSES.civora}`} target="_blank" rel="noreferrer" className="font-mono text-xs text-accent">Primary Civora contract ↗</a></section></main>;
}
